// import { type UserConfig } from '../user_app/types'
// import CurrentProject from '../utils/current_project'
// import EventHandler from './request_handler'

export const handler = async (event: any): Promise<any> => {
  // const appConfig: UserConfig = await CurrentProject.getApplicationConfig(false)
  // const app = new EventHandler(appConfig)
  // const response = await app.handleEvent(event)
  // return response
  throw new Error('Not implemented')
}
