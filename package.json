{
  "name": "apigatewayhandler",
  "version": "1.0.0",
  "description": "Lambda function generated by Amplify",
  "main": "dist/index.js",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "ts": "NODE_ENV=local ts-node",
    "clean:build": "rm -rf ./dist",
    "clean": "rm -rf ./node_modules && npm run clean:build"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
