{
  "name": "palico-app",
  "version": "0.0.2",
  "main": "src/index.js",
  "license": "Apache-2.0",
  "publishConfig": {
    "@palico-ai:registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: No Tests\" && exit 0",
    "build:watch": "tsc -w",
    "ts": "NODE_ENV=local ts-node",
    "clean:build": "rm -rf **/*.js && rm -rf **/*.d.ts && rm -rf **/*.js.map",
    "clean": "rm -rf ./node_modules && npm run clean:build"
  },
  "bin": "./src/cli/index.js",
  "dependencies": {
    "archiver": "^6.0.1",
    "axios": "^1.6.3",
    "commander": "^11.1.0",
    "json-schema": "^0.4.0",
    "openai": "^4.24.1",
    "zip-a-folder": "^3.1.5"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.2",
    "@types/json-schema": "^7.0.15",
    "@types/node": "^20.2.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
